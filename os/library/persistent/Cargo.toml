cargo-features = ["edition2024"]

[package]
edition = "2024"
name = "persistent"
version = "0.1.0"
authors = ["Simon Kraemer <simon.kraemer@hhu.de>"]

[dependencies]
# Local dependencies
syscall = { path = "../syscall" }
terminal = { path = "../terminal" }

# External depencies

